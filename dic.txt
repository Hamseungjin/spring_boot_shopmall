1. docker-compose -f docker-compose.yml -f docker-compose-prod.yml up --build -d
2. docker stop $(docker ps -aq); docker rm $(docker ps -aq); docker rmi -f $(docker images -q)
3. docker exec -it spring-app-prod /bin/bash
